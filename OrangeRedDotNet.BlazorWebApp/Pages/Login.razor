@page "/login"

<Heading Size="HeadingSize.Is2">Loginng in...</Heading>

@if (!string.IsNullOrWhiteSpace(Error))
{
    <Heading Size="HeadingSize.Is2">Error</Heading>
    <Paragraph>@Error</Paragraph>
}
